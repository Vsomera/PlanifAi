// import axios from "axios"

export const fetchNearbyRestaurants = ( longitude : number, latitude : number ) => {
    // returns a list of restaurants near a given coordinate
    return { longitude, latitude}
}