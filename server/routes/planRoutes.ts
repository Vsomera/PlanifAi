import express from "express"
const router = express.Router()

// Create a plan
// Change the name of the plan
// Delete a plan

// Add a place to the itinerary
// Edit items the Itinerary
// Delete items in the Itinerary

// Set the date of a place in the itinerary

module.exports = router